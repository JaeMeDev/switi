import styled from '@emotion/styled';
import { getColor } from '@utils/themeUtils';

export const ListWrapper = styled.div`
  margin-top: 36px;
`;

export const NoNotice = styled.p`
  font-size: 14px;
  font-weight: 500;
  font-style: normal;
  color: ${getColor('black')};
`;
